# Gmail Configuration
# You need to enable 2-Factor Authentication and create an App Password
# Visit: https://myaccount.google.com/apppasswords
GMAIL_USER=your.email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# SMTP Configuration (Gmail defaults - usually no need to change)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# IMAP Configuration (Gmail defaults - usually no need to change)
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# Email Infrastructure Server Port
EMAIL_SERVER_PORT=3001

# Optional: Custom sender name (defaults to "Porto Auth")
# EMAIL_FROM_NAME="Porto Auth"

# Optional: Debug logging level (info, debug, error)
# LOG_LEVEL=info